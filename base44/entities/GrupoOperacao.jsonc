{
  "name": "GrupoOperacao",
  "type": "object",
  "properties": {
    "nome": {
      "type": "string",
      "title": "Identificador"
    }
  },
  "required": [
    "nome"
  ],
  "rls": {
    "read": {
      "created_by_id": "{{user.id}}"
    },
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "update": {
      "created_by_id": "{{user.id}}"
    },
    "delete": {
      "created_by_id": "{{user.id}}"
    }
  }
}